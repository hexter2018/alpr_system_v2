fastapi==0.115.6
uvicorn[standard]==0.32.1
python-multipart==0.0.12
pydantic==2.10.3
pydantic-settings==2.6.1
SQLAlchemy==2.0.36
psycopg2-binary==2.9.10
alembic==1.14.0
redis==5.2.0
celery==5.4.0
psutil==6.1.0
Pillow==10.4.0
tzdata>=2024.1

# ── HTTP client (Telegram, outbound webhooks) ─────────────────────────────────
httpx>=0.27.0

# ── Authentication & security ─────────────────────────────────────────────────
# bcrypt must be pinned to 3.2.2 — passlib has no awareness of the
# __about__ removal in bcrypt 4.0+, which causes AttributeError at import
# and a secondary ValueError ("password cannot be longer than 72 bytes")
# at runtime.  Remove the pin only after migrating away from passlib
# (e.g. to pwdlib or passlib's successor).
python-jose[cryptography]>=3.3.0
passlib==1.7.4
bcrypt==3.2.2

# ── Worker / ML dependencies (shared via docker build context) ────────────────
opencv-python-headless==4.10.0.84
numpy==2.1.3
rapidfuzz==3.10.1
# Optional (for real YOLO): ultralytics + torch
ultralytics==8.3.30
cuda-python>=12.0.0
